import { createSelector } from 'reselect';

const selectPartOfAppState = (state) => state.get({{#if reducer}}'{{reducer}}'{{else}}'<REDUCER_NAME_HERE>'{{/if}});

const select{{uppercaseFirst selectorName}} = () => createSelector(
  selectPartOfAppState(),
  // you can add more selector here ...
  (partOfState) => partOfState
);

export default select{{uppercaseFirst selectorName}};